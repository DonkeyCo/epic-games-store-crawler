export type CatalogVar = {
    allowCountries: string,
    category: string,
    count: number,
    country: string,
    keywords?: string,
    locale: string,
    releaseDate?: string,
    sortBy?: string,
    sortDir?: string,
    start?: number,
    tag?: string,
    withPrice?: Boolean
}